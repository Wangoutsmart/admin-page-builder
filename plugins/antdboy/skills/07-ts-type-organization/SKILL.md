---
name: ts-type-organization
description: React + Ant Design + ahooks 中后台项目的 TypeScript 类型组织规范。用于生成、修改或重构页面、表单、请求 hook、模块类型和全局类型时，统一约束类型定义的放置位置和分层方式。默认要求类型按作用域就近放置，接口契约类型靠近请求逻辑，表单类型靠近页面或表单组件，模块共享业务类型放在模块级 `types.ts`，只有跨模块复用的基础泛型和通用结构才放到 `src/types`。不用于请求 hook 选型、payload 组装、表单校验规则或权限控制。
user-invocable: false
---

# TypeScript 类型组织规范

## 说明

使用这份规范统一处理中后台前端项目中的 TypeScript 类型分层和放置方式。
默认让类型定义尽量靠近其实际使用位置，减少全局类型文件膨胀和接口类型、表单类型混用的问题。

### TypeScript 类型定义规范

- 类型定义按作用域就近放置，不要默认全部抽到全局公共文件。
- 页面私有类型、组件私有类型、局部返回值类型优先放在当前文件或当前模块附近。
- 当同一模块下多个页面、弹窗、hook 需要复用同一批业务类型时，再抽到模块级 `types.ts`。
- 只有跨模块复用的基础泛型、通用响应结构、通用选项类型，才放到 `src/types` 这类全局类型目录。

### 类型分层规范

- 接口入参、出参类型优先跟随请求逻辑放置，放在对应请求 hook、请求文件或模块级 `types.ts` 附近。
- 表单类型、查询条件类型、弹窗表单类型应和页面或表单组件放在一起，不要直接和接口类型混用。
- 表单 `values` 类型不要直接复用接口入参类型；当表单字段结构和接口字段结构不一致时，分别定义类型。
- 不要把页面私有类型、表单类型、接口契约类型全部堆到一个公共文件里。
- 不要因为“方便统一管理”就把所有类型都塞进 `apis.ts`；`apis.ts` 或请求 hook 文件只承载与接口契约强相关的类型。

### 命名规范

- 请求参数类型命名为 `XxxQueryParams`、`XxxCreateParams`、`XxxUpdateParams`
- 响应数据类型命名为 `XxxItem`、`XxxDetail`、`XxxResponse`
- 表单类型命名为 `XxxFormValues`、`XxxSearchFormValues`
- 组件属性类型命名为 `XxxProps`
- 不要使用 `Data`、`Info`、`Item` 这类过于泛化且没有业务含义的命名

### interface 和 type 使用规范

- 对象结构、组件 props、接口契约优先使用 `interface`
- 联合类型、交叉类型、工具类型、函数签名优先使用 `type`
- 不要在同一类结构上随意混用 `interface` 和 `type`，保持项目内风格一致

### 类型安全规范

- 禁止使用 `any`
- 当类型暂时无法直接收敛时，优先使用 `unknown`，再通过类型守卫、类型收窄或局部断言处理
- 不要把“不确定”直接处理成可选字段或 `any`
- 可选字段 `?` 只用于真正允许缺省的场景，不要为了省事把大量字段都写成可选

### 枚举与工具类型规范

- 前端状态值、标签值、固定取值集合，优先使用字符串联合类型，不默认使用 `enum`
- 善用 `Pick`、`Omit`、`Partial`、`Record` 等工具类型复用已有结构，但不要为了复用把类型写得难以阅读
- 只有当类型关系明确且能提升可维护性时，才使用复杂工具类型，不要机械套用

### 默认约束

- 类型定义应优先表达业务含义，而不是只描述数据形状
- 类型名称应清晰反映所属业务和用途
- 如果一个类型需要长篇注释才能理解，优先先优化命名和拆分结构，而不是依赖注释兜底
